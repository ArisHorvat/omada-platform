import { StyleSheet } from 'react-native';

/** Shared scroll padding for single-column org admin workspaces. */
export const adminWorkspaceScrollContent = StyleSheet.create({
  scroll: {
    paddingHorizontal: 16,
    paddingBottom: 120,
  },
}).scroll;
